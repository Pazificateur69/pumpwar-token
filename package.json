{
  "name": "pumpwar-token",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PumpWar $PWAR — SPL utility token (mint, metadata, fixed supply, revoked authorities).",
  "scripts": {
    "create": "tsx createToken.ts"
  },
  "dependencies": {
    "@metaplex-foundation/mpl-token-metadata": "^3.3.0",
    "@metaplex-foundation/mpl-toolbox": "^0.9.4",
    "@metaplex-foundation/umi": "^0.9.2",
    "@metaplex-foundation/umi-bundle-defaults": "^0.9.2",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
